# OpenCode goal-plugin runtime state (unrelated to the runner source)
.opencode/

# Compiled binary
/global-build
/dist/

# Leftover build/run artifacts emitted by freshness/runner helper scripts
fresh.err
fresh.out
